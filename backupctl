#!/usr/bin/env python3
import sys
from pathlib import Path

HERE = Path(__file__).resolve().parent
if not (HERE / 'homelab_backup').is_dir():
    sys.path.insert(0, str(HERE.parent / 'lib' / 'homelab-backup'))

from homelab_backup.cli import main

if __name__ == '__main__':
    main()
