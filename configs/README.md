# Disaster recovery configuration files

Run `sudo ../backup-configs.sh` to populate this directory with:

- `restic-password`
- `rclone.conf`
- `config.yaml`

These files contain secrets. Do not publish them. Use only a tightly controlled private repository, an encrypted password manager, or offline encrypted storage.
